<% if user_signed_in? %>

<h3>You have succesfully signed in</h3>
<% @user = current_user %>
<%= render template: "home/user_details" %>

<%= link_to "Edit Profile", edit_user_registration_path(user_id: current_user.id), :method => :get %>
&nbsp 
&nbsp

<%= link_to "Sign Out", destroy_user_session_path, :method => :delete %>
&nbsp 
&nbsp
<%= link_to "Add Hobbies", new_user_hobby_path(user_id: current_user.id), :method => :get %>
&nbsp
&nbsp
<%= link_to "Show Hobbies", user_hobbies_path(user_id: current_user.id), :method => :get %>

<% else %>

<h1> Welcome to Rails Community</h1>
<%= link_to "Sign In", new_user_session_path, :method => :get %>
&nbsp
&nbsp
&nbsp
&nbsp
<%= link_to "Sign up", new_user_registration_path, :method => :get %>

<% end %>


